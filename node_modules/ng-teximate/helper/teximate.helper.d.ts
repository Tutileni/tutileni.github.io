export declare module Helper {
    /** Shuffle Array */
    const shuffle: (array: any) => any;
    /**
     * Process a word, returns words letters' array and the delay used to display
     * such as { ['a', 'b', 'c'], 3500 }
     */
    const processWord: (options: any, arr: any, i: any, relativeIndex: any) => {
        letters: any;
        delay: any;
    };
    /**
     * Process a letter, returns the letter content and the delay used to display
     * such as { 'a', 1500 }
     */
    const processLetter: (options: any, arr: any, i: any) => {
        item: any;
        delay: any;
    };
}
